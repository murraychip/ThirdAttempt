version: 1
frontend:
  phases:  
    preBuild: 
        -mkdir -p build
        -npm install
    build:
      commands: -npm run build
  artifacts: 
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths: []
