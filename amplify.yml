version: 1
frontend:
  phases:  
    preBuild: 
        -npm install
    build:
      commands: 
        -npm run generate
        -npm run build
    postBuld: 
        commands: []
  artifacts: 
    baseDirectory: .nuxt
    files:
      - '**/*'
  cache:
    paths: []
