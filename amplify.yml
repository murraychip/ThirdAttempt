version: 1.0
env:
 variables:
  BUILD_FOLDER: app
  OUTPUT_FOLDER: dist
frontend:
 phases:
  preBuild:
   commands: 
    - yarn install 
  build:
   commands:
    - yarn run build 
 artifacts:
  baseDirectory: /
  files:
   - '/*'
 cache:
  paths:
   - node_modules//*

