version: 1
frontend:
  phases:  
    preBuild: 
        -yarn install
    build:
      commands: -yarn run build
    postBuld: 
        commands: []
  artifacts: 
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths: []
